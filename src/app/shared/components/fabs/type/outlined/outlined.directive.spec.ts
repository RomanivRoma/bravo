import { OutlinedFabDirective } from './outlined.directive';

describe('OutlinedDirective', () => {
  it('should create an instance', () => {
    // const directive = new OutlinedDirective();
    // expect(directive).toBeTruthy();
  });
});
