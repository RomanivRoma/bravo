import { MinLogoDirective } from './min-logo.directive';

describe('MinLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new MinLogoDirective();
    expect(directive).toBeTruthy();
  });
});
