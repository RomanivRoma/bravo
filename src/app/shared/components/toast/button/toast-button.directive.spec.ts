import { ToastButtonDirective } from './toast-button.directive';

describe('ToastButtonDirective', () => {
  it('should create an instance', () => {
    // const directive = new ToastButtonDirective();
    // expect(directive).toBeTruthy();
  });
});
