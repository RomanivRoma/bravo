import { ContainedButtonDirective } from './contained.directive';

describe('ContainedDirective', () => {
  it('should create an instance', () => {
    // const directive = new ContainedDirective();
    // expect(directive).toBeTruthy();
  });
});
